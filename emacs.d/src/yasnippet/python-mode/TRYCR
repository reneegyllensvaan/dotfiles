# -*- mode: snippet -*-
# name: Try Commit/rollback
# key: TRYCR
# --
try:
    session.commit()
except Exception as e:
    log.error(
	"$1: %s", str(e),
	exc_info=True,
	include_trace=True
    )
    session.rollback()
    raise