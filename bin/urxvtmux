#!/bin/sh
if [ "$@" = "" ]; then
  tmux -f ~/.tmux-disposable.conf new-session -t "disp"
  # tmux -f ~/.tmux-disposable.conf new-session -s "disp-$RANDOM"
else
  tmux -f ~/.tmux-disposable.conf $@
fi
