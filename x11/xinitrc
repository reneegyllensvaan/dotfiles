# Source session setup (keyboard, Xresources, daemons)
# xsession handles pre-WM config via xprofile + daemon startup
[ -f "$HOME/.xsession" ] && . "$HOME/.xsession"

if [ -z "$WM" ]; then
  exec i3
else
  exec $WM
fi
