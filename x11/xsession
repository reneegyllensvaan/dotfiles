#!/bin/sh
# Used by startx / xinit. Display managers use ~/.xprofile instead.

# Source pre-WM config (keyboard, Xresources, xset)
[ -f "$HOME/.xprofile" ] && . "$HOME/.xprofile"

# Daemons (display manager users get these from i3 config exec)
dunst &
clipmenud &
